# /healthcheck
Запустить `workspace/bin/stack/healthcheck.sh` (аргумент — фильтр по имени проверки). По упавшим — хвост лога,
гипотеза причины, предложение (не чинить без запроса). Ничего на stage/prod.
