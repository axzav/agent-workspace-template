# /refine — заметки и предложения → задачи
Прочитать `workspace/tracker/notes/*` и `proposals/*`. Для каждого пункта: дубликат существующей задачи? →
дописать туда; иначе завести задачу по правилам `/tracker-add` (локальная — `trk.sh next-id`; для удалённого трекера —
сначала тикет, id = ключ), размер, repos, при нужде эпик. Обработанные proposals удалить, notes — пометить обработанные. Показать сводку до записи, дождаться «ок».
