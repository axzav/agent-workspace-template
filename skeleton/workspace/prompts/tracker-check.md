# /tracker-check
Запустить `workspace/bin/tracker/trk.sh check` и `workspace/bin/check/links.sh`. Каждое замечание — исправить
(frontmatter, расположение файла по статусу, битые ссылки) или объяснить, почему нет. Показать `trk.sh board`.
