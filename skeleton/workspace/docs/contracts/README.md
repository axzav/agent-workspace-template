# Контракты между сервисами
`events/<name>.schema.json` + `events/<name>.example.json`; REST — `rest/<service>.md`. Правило синхронности:
меняется контракт → меняются обе стороны и фикстура в **одном** изменении.
