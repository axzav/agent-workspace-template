# Разработка (командный способ)
- Стек: `{{run.team из project.yaml}}`. Адреса и порты — `project.yaml`.
- Команды сервисов — только через `make` сервиса в контейнере. Исключения на хосте: {{Playwright/e2e, smoke}}.
- Окружения: local / stage / prod — {{адреса, кто владеет}}. Ничего на stage/prod без явной просьбы.
- Личный способ запуска — `CLAUDE.local.md` (приоритет над этим документом); устройство личного слоя — `local-layer.md`.
