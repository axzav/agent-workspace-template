Командные хуки (под git). Подключаются в `.claude/settings.json` → `hooks`. Личные предохранители
(например, запрет «голого» `make`, если у тебя свой стек) — в `local/hooks/`, подключение в `settings.local.json`.
Пример личного хука — `workspace/docs/dev/local-layer.md`.
