# CLAUDE.local.md — ЛИЧНЫЕ инструкции (не в git). Скопируй в `CLAUDE.local.md` и правь.

> Этот файл **переопределяет** `AGENTS.md` (корня и сервисов) во всём, что касается запуска окружения,
> кредов и личных ограничений. Командный способ запуска описан в `AGENTS.md`; здесь — мой.

## Мой локальный стек
Запуск — **только** так (никогда `cd infra && make up` из командного способа):
```bash
{{напр.: cd backend && make -f Makefile.local up}}
```
Адреса: {{…}}. Compose-project: `{{…}}`.
Что отличается от командного стека: {{порты, bind-mount, отсутствие worktree…}}.

## Тестовые креды (только локальный dev)
- {{email / password}}

## Личные запреты и особенности
- {{напр.: НИКОГДА не создавать git worktree — стек бинд-монтирует `backend/`.}}
- {{напр.: `/tracker-handle-task` вызывать только с `--here`.}}

## Личные скрипты
`local/` (не в git): {{что там лежит}}. Хуки-предохранители — `local/hooks/`, подключены в `.claude/settings.local.json`.
