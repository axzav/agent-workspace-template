# {{SERVICE}} — инструкции для агента

> Карта всего проекта, трекер и знания — в родительской папке (`../AGENTS.md`); агент обычно запускается оттуда.
> Пути в этом файле — от корня этого репозитория. Личные переопределения — `CLAUDE.local.md` (не в git).

## Что это
{{роль сервиса, 2–3 предложения; статус: что реализовано / известный долг}}

## Запуск и команды
{{Как поднимается окружение: напр. стек поднимает `../infra` (`make up`), или `cargo build` без сервисов}}.
Команды качества — те же, что в `../project.yaml: repos[].commands`: {{`make test`, `make lint` / `cargo test`,
`cargo clippy`, `cargo bench`}}. {{Только в контейнере, на хосте не запускать php/composer/npm — или: тулчейн на хосте}}.

## Стек
{{язык/фреймворк/БД/очереди с версиями}}

## Архитектура и структура
{{слои, ключевые каталоги; ссылки на docs/*.md по областям. Monorepo: карта модулей/крейтов и
правила зависимостей между ними — кто от кого не зависит и как это проверяется сборкой}}

| Область | Док |
|---|---|
| {{auth}} | `docs/auth.md` |

## Правила кода
{{стиль, именование, что запрещено}}

## Тестирование
{{какие тесты обязательны, как запускать, что запрещено}}

## Git
База — `{{main}}`; Conventional Commits `feat({{PREFIX}}-NNN): …`; **перед коммитом спросить**; `git push` — только по
явной просьбе (подагентам — запрет push). Не редактировать устаревшие файлы: {{список, если есть}}.

## Чеклист «когда меняешь код»
- [ ] {{миграции/схемы обновлены}}
- [ ] {{тесты затронутой области}}
- [ ] {{док области в docs/ актуален}}
- [ ] контракт с другими сервисами не изменился, либо изменён в обеих сторонах
